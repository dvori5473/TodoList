{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "ConnectionStrings": {
    "todos": "server=${DB_HOST};user=${DB_USER};password=${DB_PASSWORD};database=${DB_NAME}"
  }
}
